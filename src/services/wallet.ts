/**
 * Portefeuille conducteur — montants en FCFA.
 *
 * Le solde initial permet de tester quelques courses (commission plateforme
 * de 10 % débitée à chaque course acceptée).
 */
export const INITIAL_DRIVER_BALANCE = 200;

/** Seuil d'alerte « solde faible » par défaut (FCFA). */
export const LOW_BALANCE_THRESHOLD = 500;
